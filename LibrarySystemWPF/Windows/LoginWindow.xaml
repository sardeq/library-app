<Window x:Class="LibrarySystemWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource DarkBlueBrush}">
        <TextBlock Text="Library System" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{StaticResource LightBlueBrush}"
                           Margin="10 20 10 30"
                           HorizontalAlignment="Center"/>
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Username" Margin="0 0 0 5" Style="{StaticResource WhiteTextBlockStyle}"/>
            <TextBox x:Name="txtUsername"/>
            <TextBlock Text="Password" Margin="0 10 0 5" Style="{StaticResource WhiteTextBlockStyle}"/>
            <PasswordBox x:Name="txtPassword"/>
            <Button Content="Login" Click="BtnLogin_Click" Margin="20 20 20 0" Padding="10 5"/>
        </StackPanel>
    </Grid>
</Window>